cmake_minimum_required(VERSION 2.8.1) 
project(s2plotApp)
find_package(Omegalib)
include_directories(${OMEGA_INCLUDE_DIRS})
add_executable(s2plotApp s2plotApp.cpp)
target_link_libraries(s2plotApp ${OMEGA_LIB} ${s2plot_LIB})
